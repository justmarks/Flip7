import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.flip7.scoretracker',
  appName: 'Flip7',
  webDir: 'dist',
  android: {
    path: 'Android',
  },
}

export default config
